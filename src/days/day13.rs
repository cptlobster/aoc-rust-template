/// Day 13 Code
use crate::common;

/// Solve a string for the first part of this day.
pub fn solve_1 (input: &str) -> i32 {
    0
}

/// Solve a string for the second part of this day.
pub fn solve_2 (input: &str) -> i32 {
    0
}

/// Tests for today's implementation
#[cfg(test)]
mod tests {
    use super::*;

    /// Part 1
    #[test]
    #[ignore] // delete this line when ready to use
    fn sample_1() {
        // Fill in with the examples provided
        let str1: &str = "";
        let str2: &str = "";
        let res1: i32 = 1;
        let res2: i32 = 2;

        assert_eq!(solve_1(str1), res1);
        assert_eq!(solve_1(str2), res2);
    }

    /// Part 2
    #[test]
    #[ignore] // delete this line when ready to use
    fn sample_2() {
        // Fill in with the examples provided
        let str1: &str = "";
        let str2: &str = "";
        let res1: i32 = 1;
        let res2: i32 = 2;

        assert_eq!(solve_2(str1), res1);
        assert_eq!(solve_2(str2), res2);
    }
}